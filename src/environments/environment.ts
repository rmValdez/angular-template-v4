// Production defaults. Swapped out for `environment.development.ts` in dev
// builds via the `development` fileReplacements in angular.json.
export const environment = {
  production: true,
  // Replace with your real production API origin before deploying.
  apiBaseUrl: 'https://api.yourapp.com/api',
};
